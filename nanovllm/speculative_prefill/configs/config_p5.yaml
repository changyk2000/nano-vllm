# Speculative Prefill Configuration - Keep 50% of tokens (p5)
# Conservative configuration for moderate speedup with high quality

enabled: true
keep_strategy: percentage
keep_kwargs:
  percentage: 0.5
look_ahead_cnt: 8
pool_kernel_size: null
ignore_eos: false
draft_model: null
chunk_based: false
chunk_size: 32
